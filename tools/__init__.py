"""Gate tooling for the anamnesis repository.

Each module is both an importable library (for the tests beside it) and a
standalone CLI invoked by the gate receipts described in `.github/workflows`.
"""
